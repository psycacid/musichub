<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        /* Add your CSS styles here */
        /* Style for cards */
        .card {
            width: 300px;
            border: 1px solid #ccc;
            border-radius: 5px;
            padding: 10px;
            margin: 10px;
            display: inline-block;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
        }

        /* Style for navbar */
        .navbar {
            background-color: #333;
            overflow: hidden;
        }

        .navbar a {
            float: left;
            display: block;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 20px;
            text-decoration: none;
        }

        .navbar a:hover {
            background-color: #ddd;
            color: black;
        }

        /* Add more styles as needed */
        /* Style for play button */
.card button[type="submit"] {
    width: 100%;
    padding: 10px;
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 16px;
}

.card button[type="submit"]:hover {
    background-color: #0056b3;
}

    </style>
</head>
<body>

<div class="navbar">
    <a href="/dashboard">Home</a>
    <a href="#genre">Genre</a>
    <a href="#latest">Latest</a>
    <a href="/profile">Profile</a>
    <a href="/logout" style="float: right;">Logout</a>
</div>

<h1>Latest track</h1>

<div class="music-cards">
    <% musics.forEach((music) => { %>
        <div class="card">
            <img src="images/<%= music.song_image %>" alt="<%= music.title %>" style="width: 100%;">
            <h2><%= music.title %></h2>
            <p><strong>Genre:</strong> <%= music.genre %></p>
            <!-- Play Button -->
            <form action="/play/<%= music.id %>" method="GET">
                <button type="submit">Play</button>
            </form>
        </div>
    <% }); %>
</div>
<footer class="footer fixed-bottom bg-dark text-light">
    <div class="container text-center">
        <span class="text-muted">&copy; 2024 Your Company</span>
    </div>
</footer>
</body>
</html>
